# ChordiggerApp
## このツールはα版です
SongleのAPIを利用し、コード進行で曲をdigできるツール。  
現状はFlask+HTMLのみで構成されています（CSSすら使ってないので飾り気もないです）。  
pipでflaskとrequestsを入れてserver.pyを起動すれば動くはずですが、将来的にはJSで書き直して静的ホスティング上で動かせるようにしたいと思っています…
