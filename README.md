# ChordiggerApp
## このツールはα版です
SongleのAPIを利用し、コード進行で曲をdigできるツール。  
現状はFlask+HTMLのみで構成されています（CSSすら使ってないので飾り気もないです）。  
pipでflaskとrequestsを入れてserver.pyを起動すれば動くはずですが、将来的にはJSで書き直してCSSで見た目もいい感じにしつつ静的ホスティング上で動かせるようにしたいと思っています…
### 既知の問題点
- 取得する動画の範囲が1ページ目の40件のみになっている→総ページ数を取得してその範囲内で乱数にする等、何らかの方法で対応を考える
- ニコ動のembedプレイヤーがうまく動かない→最初は動いていたのになぜ？要調査
